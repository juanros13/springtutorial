server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: zipkin-sleuth
          uri: https://github.com/juanros13/springtutorial
          search-paths: config-data
  application:
    name: config-service